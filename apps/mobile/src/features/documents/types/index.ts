/**
 * Documents & Waivers Types
 * Encrypted medical clearances, liability waivers, and contract archives.
 */

export interface DocumentsState {
  isInitialized: boolean;
  isLoading: boolean;
  error: string | null;
}

/**
 * Role Dashboards Types
 * Aggregated analytics and operational summaries tailored to user role.
 */

export interface DashboardState {
  isInitialized: boolean;
  isLoading: boolean;
  error: string | null;
}
